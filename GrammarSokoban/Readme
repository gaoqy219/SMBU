启动服务：
npm run dev:server
npm run dev


依赖说明：
服务器是一个 Node.js 应用，只需要 Node.js 即可运行，没有其他系统依赖。

  需要安装的

  Node.js（v18 或以上）：https://nodejs.org

  移动过去后执行

  cd GrammarGamePlatform
  npm install
  npm run db:seed        # 初始化数据库（只需一次）
  npm run dev:server     # 启动游戏服务器

  不需要的

  - 不需要数据库软件（sql.js 是纯 JS 的 SQLite，内嵌在 npm 包里）
  - 不需要 Python、C++ 编译器（sql.js 是 WASM 的，无需原生编译）
  - 不需要额外安装任何东西

  前端可以同一台机器跑 npm run dev，也可以只跑服务器，学生连 http://{Windows_IP}:3000、教师用 http://localhost:3000 访问。

  如果 Windows 上只想跑服务器（不需要前端开发热更新），改一下启动命令即可：

  # 先构建前端
  npm run build

  # 用生产模式启动（Express 会直接 serve 构建好的前端）
  set NODE_ENV=production
  npm run dev:server

  这样只需要一个命令，教师和学生都访问同一个端口 3000。




核心要义：通过游戏让学生练习汉语语法（句法成分的排列顺序）

语法库：制作一个数据库，采集汉语语法，并用句法成分的方式表达，比如 S+V；S+V+O；定+S+V；定+S+状语+V+定+O；主（施事）+“把“+宾语（受事）+V+处置结果；S+一+v1+就+v2等等。每个语法表达式配有拆分好句法成的的5个例句。语法库可以通过web后台进行编辑（包括增加、修改、删除语法表达式、为指定语法表达式增加、修改、删除例句（按照不同的句法成分以此添加）。需要给出seed数据。这些数据必须保证准确无误。

游戏准备：教师凭密码（SMBUCLC）主持游戏，开启房间（会有随机码），设置队伍数量（1-5个队伍）和局数（1、3、5、7、9局），每个队伍1-5个成员。教师开启房间后，会出现二维码，学生端扫二维码或输入随机码进入房间准备，准备页面可以选择队伍、角色的颜色（每组不可有相同的颜色）和最多三个字母的角色名称（名称显示在角色身上）。第一个进入队伍的学生自动成为队长。所有学生准备后，老师可以开始游戏。

游戏开始：游戏开始后会根据设定的局数随机从语法库里选取对应数量的语法表达式，连同该表达式的例句（句法成分组），如果是多队伍游戏，不同队伍在同一局次将获得相同的语法表达式，但将获得不同的例句。游戏过程以推箱子游戏为基底，每个队伍一个场地30*30，场地内随机分布箱子（不靠墙）、障碍物（不可移动），每个箱子随机分配一个句法成分（不可重复），目标地块排成一横排或一纵排。学生需要用上下左右键控制角色推动箱子，把所有箱子按照正确的语序推到目标地块即成功过关。过关后即可进入下一局，最先完成所有局数的队伍获胜，此时所有游戏结束，进入结算页面，结算页面会出现leaderboard，并显示不同队伍完成的局数。屏幕弹幕随机滚动游戏随机抽取的所有例句（完整的连成句子的例句）

注意：1、队长可以重新刷新地图，以防止随机刷出的障碍物的位置导致游戏无法完成。2、游戏启动在服务器里，教师端和学生端在同一局域网里连接服务器，所以服务器地址是内网的地址，而是不127.0.0.1这种，要考虑网络畅通性。3、游戏要做好美工，句法成分、角色名称要清晰显示。4、学生端多为手机，需要考虑手机上的操作的适用性。



在 Windows 11 上部署语法推箱子（Grammar Sokoban）
  
  一、安装 Node.js

  1. 打开浏览器，访问 https://nodejs.org
  2. 下载 LTS 版本（左侧绿色按钮，版本号 ≥ 18，比如 22.x LTS）
  3. 运行下载的 .msi 安装程序，一路点"下一步"即可，所有选项保持默认
  4. 安装完成后，按 Win + R，输入 cmd 回车，在黑色窗口里输入以下命令验证安装：

  node -v
  npm -v

  4. 如果都显示版本号（如 v22.12.0 / 10.9.0），说明安装成功。

  ---
  二、拷贝项目到 D 盘
  
  1. 将 U 盘里的 GrammarSokoban 文件夹整个拖到 D 盘根目录
  2. 最终路径应该是：D:\GrammarSokoban

  ---
  三、安装项目依赖
  
  1. 按 Win + R，输入 cmd 回车，打开命令提示符
  2. 逐条执行以下命令：

  cd /d D:\GrammarSokoban
  npm install

  2. npm install 会根据 package.json 自动下载所有依赖到 node_modules 文件夹，大约需要 1-3 分钟，等待完成即可。

  ---
  四、初始化数据库（只需做一次）
  
  在同一个命令行窗口继续执行：

  npm run db:seed

  这会创建游戏所需的 SQLite 数据库文件并写入种子数据（语法表达式和例句）。

  ---
  五、启动服务
  
  有两种方式，推荐方式 B（更简单）：

  方式 A —— 开发模式（需要开两个命令行窗口）

  窗口 1 —— 启动后端服务器（端口 3000）：
  cd /d D:\GrammarSokoban
  npm run dev:server
  
  窗口 2 —— 启动前端开发服务器（端口 5173）：
  cd /d D:\GrammarSokoban
  npm run dev
  
  方式 B —— 生产模式（推荐，只需一个窗口）

  cd /d D:\GrammarSokoban
  npm run build
  set NODE_ENV=production
  npm run dev:server

  服务器启动后会自动检测本机局域网 IP，终端会打印类似：
    ╔══════════════════════════════════════╗
    ║   语法推箱子 Grammar Sokoban Server    ║
    ╠══════════════════════════════════════╣
    ║  Local:   http://localhost:3000      ║
    ║  Network: http://192.168.x.x:3000    ║
    ╚══════════════════════════════════════╝

  ---
  六、访问游戏
  
  ┌──────────────────────────────────┬────────────────────────────────────────────────────┐
  │               角色                │                      访问地址                       │
  ├──────────────────────────────────┼────────────────────────────────────────────────────┤
  │ 教师端（本机浏览器）                 │ http://localhost:3000                              │
  ├──────────────────────────────────┼────────────────────────────────────────────────────┤
  │ 学生端（手机/平板，连同一 WiFi）      │ http://192.168.x.x:3000（终端显示的 Network 地址）     │
  └──────────────────────────────────┴────────────────────────────────────────────────────┘

  教师密码：SMBUCLC

  ---
  常见问题
  
  - Windows 防火墙弹窗：首次启动时如果弹出防火墙提示，勾选"专用网络"后点"允许访问"
  - 学生连不上：确保学生设备和 Windows 电脑连的是同一个路由器/WiFi，且用的是 Network 地址而非 localhost
  - 端口被占用：如果 3000 端口被其他程序占用，可以指定其他端口：set PORT=3001 && npm run dev:server
  - 每次使用：后续使用时只需从第五步开始（先 cd /d D:\GrammarSokoban，再启动服务），不需要重复执行第 3、4 步
